# What CIR-01 asks for. The resolved truth is requirements.lock.
#
# torch is installed first and pinned to the same version the mechlens venv runs,
# so the prototype measures the same kernels the real app would. The aarch64
# cu130 wheel is on plain PyPI -- no special index.
torch==2.14.0

# Pinned by commit, not branch: the go/no-go report has to name an artifact that
# still exists next month. 7f66876 is HEAD of master on 2026-09-11.
circuit-tracer @ git+https://github.com/decoderesearch/circuit-tracer@7f66876689f59e92fc3641650d38b8bd41749ec4
